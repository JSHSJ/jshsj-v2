---
const { content } = Astro.props
---

{content.rich_text.map(part => {
  switch (part.type) {
    case "text": {
      return <p>{part.text.content}</p>
    }
    default: {
      return
    }
  }
})}